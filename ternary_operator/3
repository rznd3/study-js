const inscritos = 2000000

const premioYoutube = inscritos >= 50000000 ? 'Rubi'
                    : inscritos >= 10000000 ? 'Diamante'
                    : inscritos >= 1000000 ? 'Ouro'
                    : inscritos >= 100000 ? 'Prata'
                    : ''