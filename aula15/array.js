let val =  [9, 1, 7, 3, 5]

val.sort()


// for (let position = 0; position < val.length; position++){
//     console.log(`Position ${position} has the value ${val[position]}`)
// }

for (let pos in val) {
    console.log(`Position ${position} has the value ${val[position]}`)
}